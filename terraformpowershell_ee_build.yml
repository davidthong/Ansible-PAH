---
- name: Sync all PAH EE and Remote Repos
  hosts: localhost
  gather_facts: false
  become: false
  tags: sync

  roles:
    - sync_ee
    - sync_repos

- name: Build new terraformpowershellee
  hosts: aapsinglenode.dthong.lan
  tags: buildee

  roles:
    - build_terraformpowershellee
